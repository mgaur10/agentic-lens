# Agentic-Prism Global Configuration (NEW PROJECT)
# From migration/: cp config/versions.env.template app/versions.env

PROJECT_ID=REPLACE_ME_PROJECT_ID
REGION=us-west1

# Numeric organization ID (required for Agent Identity IAM).
# gcloud projects describe PROJECT_ID --format='value(parent)' → organizations/123456789
ORG_ID=REPLACE_ME_ORG_ID

ADK_VERSION=1.18
MODEL_VERSION=gemini-2.5-pro

# --- CMEK (fill after Phase 1 step 05 or org-provided keys) ---
# AGENT_ENGINE_KMS_KEY_SUPERVISOR=projects/PROJECT_NUMBER/locations/REGION/keyRings/.../cryptoKeys/...
# AGENT_ENGINE_KMS_KEY_CHAT=...
# AGENT_ENGINE_KMS_KEY_ENGINEERING=...
# AGENT_ENGINE_KMS_KEY_XRAY=...
# AGENT_ENGINE_KMS_KEY_EVENTS=...

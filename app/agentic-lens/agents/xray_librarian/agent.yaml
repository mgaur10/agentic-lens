# X-Ray Librarian — Secure Fetcher
# Tools and instruction are in src/librarian.py and src/github_tools.py.
# Deployment identity is enforced by adk.yaml (AGENT_IDENTITY).
# IAM: optional roles/secretmanager.secretAccessor on github-pat-token if you store a PAT (private repos / rate limits).
name: xray-librarian
model: gemini-2.5-pro
description: Secure Fetcher — GitHub context via PyGithub (optional PAT; public repos work without one).

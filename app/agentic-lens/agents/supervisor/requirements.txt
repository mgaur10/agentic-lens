# Core SDKs (Agent Engine requires google-adk>=1.5.0)
google-cloud-aiplatform>=1.70.0
google-adk>=1.5.0
deprecated>=1.2.0

# Utilities (explicitly include for Supervisor to avoid any ADK/serialization issues)
pydantic>=2.9.0
cloudpickle>=2.0.0,<3

# Observability (Lens distributed tracing)
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-gcp-trace
opentelemetry-exporter-otlp-proto-http
opentelemetry-instrumentation-vertexai

# Supervisor-specific (Model Armor runs in Cloud Run API before supervisor; not needed in agent)
# google-cloud-modelarmor>=0.3.0
